pipeline {
    agent none;
    options {
        ansiColor('xterm')
        buildDiscarder(logRotator(daysToKeepStr: '7'))
    }
    stages {
        stage('vSphere Cleanup') {
            agent any
            steps {
            library identifier: "10.70.35.218@${params.JOBS_REPOSITORY_BRANCH}", retriever: legacySCM(scm)
                echo 'vsphere cleanup'
            }
        stage('Build') {
            steps {
                echo 'Building..'
            }
        }
        stage('Test') {
            steps {
                echo 'Testing..'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying....'
            }
        }

        }
    }
}